/* Minio Inspired Keycloak Login Theme (for Keycloak 16.1.1) */

:root {
  --minio-bg-color: #002a37;
  --minio-text-color: #ffffff;
  --minio-input-border-color: rgba(255, 255, 255, 0.1);
  --minio-input-focus-border-color: #dfdfdf;
  --minio-button-border-color: #ffffff;
  --minio-link-color: #46a5e0;
  --minio-footer-text-color: rgba(255, 255, 255, 0.4);
}

/* --- 全体的なスタイル --- */
html, body {
  height: 100%;
  background-color: var(--minio-bg-color) !important;
}

body {
  color: var(--minio-text-color) !important;
  font-family: 'Lato', sans-serif; /* Ensure Lato font is loaded */
  display: flex;
  flex-direction: column; /* Ensure footer stays at bottom */
  min-height: 100vh;
}

/* --- ログインコンテナ --- */
.login-pf-page, /* PatternFly based layout in older versions? */
#kc-container-wrapper /* Older generic container? */ {
  background: none !important;
  flex-grow: 1; /* Take available vertical space */
  display: flex;
  justify-content: center;
  align-items: center;
}

#kc-content-wrapper, /* Wraps the main content box */
.login-pf-page .card-pf /* Card element in PF based themes */ {
  background-color: transparent !important;
  border: none !important;
  box-shadow: none !important;
  width: 80%;
  max-width: 500px;
  padding: 20px; /* Add some padding inside the card */
  text-align: center;
  color: var(--minio-text-color) !important;
}

#kc-header, #kc-header-wrapper {
  display: none !important; /* Hide default header */
}

#kc-form, #kc-form-login {
    width: 100%; /* Ensure form takes full width of its container */
}

/* --- フォーム要素 --- */
.form-group {
  margin-bottom: 40px !important;
  position: relative;
  text-align: center; /* Center items within the group */
}

/* Label - Floating effect attempt */
label, .control-label {
  position: absolute;
  left: 0;
  bottom: 35px; /* Position above the input line initially */
  width: 100%;
  text-align: center;
  color: var(--minio-text-color);
  opacity: 0.7;
  font-size: 13px;
  transition: all 250ms;
  pointer-events: none;
  font-weight: 400;
  margin-bottom: 0 !important; /* Override default margins */
  padding-bottom: 0 !important;
}

/* Input field */
input[type="text"], input[type="password"], input[type="email"], .form-control {
  background-color: transparent !important;
  border: none !important;
  border-bottom: 1px solid var(--minio-input-border-color) !important;
  border-radius: 0 !important;
  color: var(--minio-text-color) !important;
  height: 40px !important;
  text-align: center !important;
  padding: 5px !important;
  box-shadow: none !important;
  width: 100% !important;
  font-size: 13px !important;
  position: relative; /* Make sure input is interactable */
  z-index: 1;
}

/* Label transition when input has value or focus */
input[type="text"]:focus + label,
input[type="password"]:focus + label,
input[type="email"]:focus + label,
input[type="text"]:not(:placeholder-shown):valid + label, /* Check if non-empty and valid */
input[type="password"]:not(:placeholder-shown):valid + label,
input[type="email"]:not(:placeholder-shown):valid + label,
/* Fallback for older browsers/simpler check */
input[type="text"][value]:not([value=""]) + label,
input[type="password"][value]:not([value=""]) + label,
input[type="email"][value]:not([value=""]) + label {
  bottom: 35px; /* Keep it above */
  /* Optionally slightly change style */
  /* font-size: 11px; */
  /* opacity: 1; */
}
/* Ensure label doesn't overlap input text */
input[type="text"]:focus, input[type="password"]:focus, input[type="email"]:focus {
  /* Maybe add slight top padding if needed, but border-bottom focus is main */
}


/* Input field focus underline */
input[type="text"]:focus, input[type="password"]:focus, input[type="email"]:focus, .form-control:focus {
  border-bottom-color: var(--minio-input-focus-border-color) !important;
  box-shadow: none !important;
  outline: none !important;
}

/* Chrome autofill fix */
input:-webkit-autofill,
input:-webkit-autofill:hover,
input:-webkit-autofill:focus,
input:-webkit-autofill:active {
    -webkit-box-shadow: 0 0 0 50px var(--minio-bg-color) inset !important;
    -webkit-text-fill-color: var(--minio-text-color) !important;
    caret-color: var(--minio-text-color);
    transition: background-color 5000s ease-in-out 0s;
}

/* --- Buttons --- */
#kc-form-buttons { /* Button container */
  text-align: center;
  margin-top: 20px;
}

#kc-login, /* Specific ID for login button */
.btn-primary { /* Bootstrap primary button */
  background-color: transparent !important;
  border: 1px solid var(--minio-button-border-color) !important;
  color: var(--minio-text-color) !important;
  border-radius: 2px !important;
  opacity: 0.6;
  transition: opacity 300ms;
  width: auto !important;
  min-width: 100px;
  padding: 10px 20px !important;
  font-size: 14px !important;
  font-weight: normal !important;
  box-shadow: none !important;
  text-transform: none !important; /* Prevent uppercase */
}

#kc-login:hover, .btn-primary:hover {
  opacity: 1;
  background-color: transparent !important;
  border-color: var(--minio-button-border-color) !important;
  color: var(--minio-text-color) !important;
}

/* --- Links --- */
#kc-form-options a, /* Links like "Forgot Password?" */
.link-primary /* Another possible link class */ {
  color: var(--minio-link-color) !important;
  font-size: 13px;
}
#kc-form-options a:hover, .link-primary:hover {
  color: var(--minio-text-color) !important;
  text-decoration: none !important;
}

/* --- Alerts / Messages --- */
.alert, .alert-error, .alert-warning, .alert-success, #kc-feedback-wrapper /* Generic alert/feedback wrappers */ {
  background-color: rgba(255, 255, 255, 0.1) !important;
  border: none !important;
  border-left: 3px solid var(--minio-link-color) !important;
  color: var(--minio-text-color) !important;
  margin: 15px 0;
  padding: 10px 15px !important;
  border-radius: 0 !important;
}
.alert span, #kc-feedback-wrapper span { /* Text inside alert */
    color: var(--minio-text-color) !important;
}
.alert .pficon /* Icon if present */ {
     color: var(--minio-link-color) !important;
}


/* --- Footer --- */
#kc-footer-wrapper {
  padding: 0 50px;
  height: 110px;
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: var(--minio-footer-text-color) !important;
  font-size: 20px;
  font-weight: 400;
  margin-top: auto; /* Push footer to bottom in flex container */
  flex-shrink: 0; /* Prevent footer from shrinking */
}

#kc-footer-wrapper ul { /* Hide default footer links */
  display: none;
}

/* Add Minio logo to footer */
#kc-footer-wrapper::after {
  content: '';
  display: block;
  width: 40px;
  height: 40px;
  background-image: url(../img/logo.svg); /* Adjust path if needed */
  background-size: contain;
  background-repeat: no-repeat;
}

/* Add Server info to footer (example with fixed text) */
#kc-footer-wrapper::before {
  content: "Your Server Info"; /* Replace with dynamic value if possible */
  display: block;
  color: var(--minio-footer-text-color);
}

/* --- Social / Identity Providers --- */
#kc-social-providers { /* Container */
    text-align: center;
    margin-top: 30px;
}

#kc-social-providers hr { /* "or" divider */
    display: block;
    margin: 30px auto;
    width: 50px;
    border: none;
    border-top: 1px solid grey;
    position: relative;
    overflow: visible;
}
#kc-social-providers hr::before {
    content: "or";
    position: absolute;
    top: -0.7em;
    left: 50%;
    transform: translateX(-50%);
    background: var(--minio-bg-color);
    padding: 0 10px;
    color: grey;
}

#kc-social-providers ul { /* List containing buttons */
    list-style: none;
    padding: 0;
    margin: 0;
}
#kc-social-providers li {
    display: inline-block;
    margin: 5px;
}

#kc-social-providers a, /* Social buttons are usually links */
.btn-social, .zocial /* Common classes for social buttons */ {
  display: inline-block !important;
  color: var(--minio-link-color) !important;
  border: 1px solid var(--minio-link-color) !important;
  opacity: 0.6;
  font-size: 14px !important;
  background: none !important;
  box-shadow: none !important;
  text-transform: none !important;
  padding: 10px 20px !important;
  border-radius: 2px !important;
  transition: opacity 300ms;
  width: auto !important; /* Override fixed widths */
  height: auto !important; /* Override fixed heights */
  line-height: normal !important; /* Override line heights */
}
#kc-social-providers a i, #kc-social-providers a span { /* Icons/text inside social buttons */
    color: var(--minio-link-color) !important; /* Ensure inner text/icon matches */
}


#kc-social-providers a:hover, .btn-social:hover, .zocial:hover {
  opacity: 1;
  color: var(--minio-link-color) !important;
  background: none !important;
  border-color: var(--minio-link-color) !important;
}

/* --- Responsive adjustments --- */
@media (max-width: 768px) { /* Standard bootstrap breakpoint */
  #kc-footer-wrapper {
    flex-direction: column;
    height: auto;
    padding: 20px;
  }
  #kc-footer-wrapper::before, #kc-footer-wrapper::after {
      margin-bottom: 10px;
  }
   #kc-footer-wrapper::before {
      order: 2;
      font-size: 15px;
  }
   #kc-footer-wrapper::after {
       order: 1;
   }
   #kc-content-wrapper, .login-pf-page .card-pf {
       width: 90%; /* Slightly wider on small screens */
       padding: 15px;
   }
}

/* Hide potentially unwanted elements */
#kc-locale, /* Language selector */
#kc-info /* Info sidebar */ {
   /* display: none !important; */ /* Uncomment to hide */
}

